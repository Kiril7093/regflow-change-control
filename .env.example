# Local dev settings
DEBUG=1
DJANGO_SECRET_KEY=replace-me-with-a-random-secret
ALLOWED_HOSTS=localhost,127.0.0.1

# Postgres (Docker Compose)
POSTGRES_DB=regflow
POSTGRES_USER=regflow
POSTGRES_PASSWORD=regflow
POSTGRES_PORT=5432

# Django DB URL (used by dj-database-url)
DATABASE_URL=postgresql://regflow:regflow@localhost:5432/regflow
